class ApiConfig {
  const ApiConfig._();

  // Replace this with your real backend domain.
  static const String baseUrl = 'https://chamcong.kimlongstore.site/api/v1';

  static const Duration connectTimeout = Duration(seconds: 15);
}
